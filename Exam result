#include <stdio.h>
int main()
{
    int marks;
    printf("Enter your marks: \n");
    scanf("%d", &marks);
    if( marks > 30){
        printf("You are passed good job!");
    }
    else{
        printf("You failed the exam, try again next time");
    }
    return 0;
}
